Original prompt: 我們將繼續開發這款名為「人頭老鼠」的 3D 手機網頁遊戲原型。為了在手機瀏覽器上達到極致的載入速度與極低的運算負擔，我決定全盤採用「復古 PS1 風格」（Retro PS1 style）與低多邊形像素藝術的視覺表現。請你作為資深圖形程式設計師，使用 Three.js 為我撰寫一段基礎的場景與視覺渲染程式碼（Proof of Concept），並嚴格遵守以下四項效能優化與視覺規範：一、停用紋理平滑過濾。二、採用無光照材質。三、實作 2D 廣告牌技術。四、提供完整單頁 HTML。並使用 asset/photos 的牆壁塗鴉圖片貼在建築表面上，修正老鼠控制邏輯、bite 功能與清潔人員清潔行為，同時規劃技術堆疊與模組，並提供自拍動態臉部貼圖與寄生控制轉移的 POC。

- 2026-05-13: 確認專案為單檔 `index.html`，既有 PS1 低解析渲染、自拍 modal、寄生切換、Sprite 場景骨架已存在。
- 2026-05-13: 發現塗鴉載入清單錯誤，程式硬編碼 `5.jpg/6.jpg/7.jpg`，實際素材為 `asset/photos/*.png`。
- 2026-05-13: 發現 cleaner 目前只有不同移動速度，沒有 tidy 狀態機；需要補上 seek/tidy 行為與 chaos 回收。
- 2026-05-13: 下一步將修正貼圖名單、bite 提示/距離、cleaner 行為與測試 hook。
- 2026-05-13: 已把 `asset/photos` 的實際 `.png` 檔名接回塗鴉載入流程，建築牆面會使用使用者提供的塗鴉素材。
- 2026-05-13: 已加入 cleaner 的 `seek -> tidy -> restore` 流程、prop 回正與 chaos 降低邏輯，並保證每個 chunk 至少出現清潔工。
- 2026-05-13: 已加入 `window.render_game_to_text()` 與 `window.advanceTime(ms)` 測試 hook，並用 Node 對抽出的 module script 做過語法檢查。
- 2026-05-13: 馬路地板已改用貼近使用者附圖的灰色柏油顆粒紋理生成器 `makeRoadReferenceTexture()`；若之後提供實體檔案可再切為真貼圖。
- 2026-05-13: 已把人類角色載入方向切到 `player_main.glb` + 其餘 `player_*.glb` clips，並額外接上 `tripo_model_basecolor.JPEG` 當貼皮。
- 2026-05-13: 老鼠已改成 2D billboard 動畫結構與腿部擺動；若使用者將提供的參考圖另存為 `asset/photos/rat_reference.png`，程式會自動優先載入。
- 2026-05-13: 場景塗鴉覆蓋率已提高，並新增變電箱 / utility cabinet 類型物件承接塗鴉。
- 2026-05-14: 場景可見 chunk 視窗由 `1` 擴到 `3`，並移除 `ensureChunksAroundX()` 的早退，初始化與行進中都會穩定預生成前後段，減少兩端封閉感。
- 2026-05-14: 建築外皮改為白灰小方磚牆基底，ground-floor 也共用同類材質，再把塗鴉平面壓在其上。
- 2026-05-14: 攤販加入髒污帆布、木紋、金屬檯面與桶子等細節，避免純色方塊感。
- 2026-05-14: 人類 GLB 載入流程補齊 `tripo_model_normal/roughness/metallic`，並沿用 `flipY=false` + feet-to-ground 校正，讓皮膚結構更接近 `urban-legend-framework` 的作法。
- 2026-05-14: 場景延伸窗口再擴到 `4` 個 chunk，霧距同步拉遠，讓建築、地板、攤販前後持續銜接生成，降低可見終點感。
- 2026-05-14: 牆面塗鴉改成小中大型混貼；最小寬度提升到原本的 `1.5x`，最大 mural 提升到原本的 `5x`，並提高整體牆面覆蓋率。
- 2026-05-14: `generateChunk()` 改成分段式建築帶：每棟建築寬度、高度、前後退縮與色偏都不同，並保留方磚底材再疊塗鴉，讓街景更像持續延伸的夜市街廓。
- 2026-05-14: 新增密集電線杆與多層 sagging cable 曲線；每個 chunk 兩側都會生成一整列電桿，並以高低壓纜線、垂落接線與變壓箱做出纏繞感。
- 2026-05-14: 宿主模式右側操作改為 `JUMP / ATTACK / PICK UP`；可踢飛道具、擊倒鄰近人類、舉起可互動物件，第二次點按或長按 `PICK UP` 會依蓄力距離投擲。
- 2026-05-14: ????????????? sset/??????_????/*.fbx,??? asecolor_1024.jpg ??????;??????? 3 ??????????,?????? PNG ???????
- 2026-05-14: ???? python -m http.server 8123 + Playwright client ??????????????;????????? 1 ? 404 ????,????????????
- 2026-05-15: 已新增 `asset/都市傳說鼠王_男高中生` 第三組人類 variant，並產生 `basecolor_1024.jpg`；動畫接入 `發呆 / 走路 / 跑動 / 跳躍 / 踢ATTACK / 清潔姿勢`，目前 `pickup` 先 fallback 到 `踢ATTACK`，因資料夾內尚無獨立拿取動畫。
- 2026-05-15: 已新增 `asset/都市傳說鼠王_女高中生` 第四組人類 variant，並產生 `basecolor_1024.jpg`；動畫接入 `走路 / 跑動 / 跳躍 / 踢ATTACK / 清潔動作`，因缺少獨立 `idle / pickup`，目前 `idle` 先沿用 `走路.fbx`、`pickup` 設為 `null`。
- 2026-05-15: 已新增靜態場景矩陣凍結 `freezeStaticObject()`，用於 `addChunkObject()` 以降低大量靜態建築/線桿/後景的每幀更新成本。
- 2026-05-15: 已新增較細緻的 `buildDetailedScooter() / buildSedan() / buildMiniTruck()`，並接入街道生成；地板顏色同步提亮到更偏淺灰的柏油色。
- 2026-05-15: Playwright 回歸驗證這一輪未完成，client 在本地頁面上逾時找不到 `#start-btn`；目前已確認 module script `node --check` 通過，但仍有既有 404 資源錯誤待追。
- 2026-05-15: äººæ½®ç”Ÿæˆæ”¹æˆæ··åˆ `solo + 3~7 äºº group`ï¼Œwanderer æœƒæ›´ç©æ¥µåƒèˆ‡æ¸…æ½”ï¼Œcleaner ç§»å‹•é€Ÿåº¦ä¹Ÿæé«˜ï¼›äººé¡žåœ¨å‘¨é­ 10 éš»ä»¥ä¸Šåœ°é¢è€é¼ è¿«è¿‘æ™‚æœƒ panic runã€‚
- 2026-05-15: å®¿ä¸»/NPC æ­»äº¡å™´è¡€æ”¹ç‚ºå¾žèº«é«”ä¸­å¿ƒå™´å‡º `spawnBodyBlood()`ï¼›`+CHAOS` æ¼«ç•«å­—æ”¹æˆç´…æ©˜æ¼¸å±¤ã€åªæœƒå�‘ä¸Šæ¼‚ã€�ä¸�æ—‹è½‰ï¼Œä¸¦æŠŠ text canvas åŠ å¯¬ä»¥é¿å…�å·¦å�³è¢«åˆ‡ã€‚
- 2026-05-15: æ‰‹æ©Ÿç‰ˆé ‚éƒ¨ HUD é‡ç–Š CSS èˆ‡ `UI_LAYOUT_DEFAULTS.mobile`ï¼Œä¸¦å°èˆŠ localStorage layout åšè‡ªå‹•é·ç§»ï¼Œä»¥è§£æ±º `RAT CITY / CHAOS / RATS` é‡�ç–Šï¼›å�Œæ™‚æ‰‹æ©Ÿç‰ˆå•ƒå’¬ `biteTarget.y` é¡å¤–ä¸‹ç§» `0.3`ã€‚
- 2026-05-15: å·²åŸ·è¡Œæœ¬åœ° `python -m http.server 8123` + `develop-web-game` Playwright client å¿«æ¸¬ï¼Œ`state-0.json` é¡¯ç¤º cleaners total 19 / tidying 0ï¼Œä»æœ‰æ—¢æœ‰ 404 è³‡æºéŒ¯èª¤å¾…è¿½ã€‚
- 2026-05-15: æ–°å¢ž `asset/temple-idol/`ï¼ŒæŠŠå»Ÿå…§ç¥žåƒè³‡ç”¢ `Breathing Idle.fbx / Walking.fbx / tripo_model_*.JPEG` è¤‡è£½é€²ç•¶å‰å°ˆæ¡ˆï¼Œä¸¦æŽ¥ä¸Šå°ˆç”¨ `TEMPLE_IDOL_SKIN` è¼‰å…¥æµç¨‹ã€‚
- 2026-05-15: å ´æ™¯æ–°å¢ž `TEMPLE_CHUNK_IDX=8` çš„çµ‚é»žå»ŸåŸ• chunkï¼ŒåŒ…å«å¤§å»£å ´ã€�GGdove ç‰Œæ¨“ã€�å�¯é€²å…¥å»Ÿå®‡èˆ‡ä¾›æ¡Œä¸­å¤®çš„ 5x ç¥žåƒï¼›ç¥žåƒåˆå§‹ç‚ºé»ƒé‡‘æè³ªä¸¦å‡çµåœ¨ idle ç¬¬ä¸€å¹€ã€‚
- 2026-05-15: æ–°å¢ž 1000 éš»è€é¼ äº‹ä»¶èˆ‡æ¸¬è©¦æ¨¡å¼ï¼š`Shift+T` åˆ‡æ› test modeï¼Œå®¿ä¸»ä¸æœƒæ­»äº¡ä¸”ç§»å‹•é€Ÿåº¦ 3xï¼›test mode ä¸‹æŒ‰ `1` æœƒè£œé½Šè€é¼ ç¸½æ•¸åˆ° 1000ï¼Œä¹‹å¾Œè€é¼ æœƒæ¹§å…¥å»Ÿä¸­çˆ¬æ»¿ç¥žåƒï¼Œç™¼å…‰å¾Œæ¢å¾©æ­£å¸¸çš®è†šä¸¦æ’­æ”¾ `Walking.fbx`ã€‚
- 2026-05-15: Playwright å¿«æ¸¬é€™ä¸€è¼ªåœ¨ `#start-btn` éšœç¤™å¤±æ•—ï¼ŒåŽŸå› æ˜¯åŠ é€²ç¥žåƒ FBX è¼‰å…¥å¾Œ 5 ç§’å…§æŒ‰éˆ•ä»ç„¶ disabledï¼›module script `node --check` ä»é€šéŽã€‚
- 2026-05-15: ä½¿ç”¨ Blender 4.3 CLI + `tools/optimize_temple_idol.py` å°‡ç¥žåƒ `Breathing Idle.fbx / Walking.fbx` å„è‡ªé™åˆ° `2880` trianglesï¼Œä¸¦è¼¸å‡º `asset/temple-idol/Breathing Idle_low.glb` èˆ‡ `Walking_low.glb`ï¼›ç´‹ç†ä¿ç•™åœ¨ glb å…§ã€‚
- 2026-05-15: ç¥žåƒè¼‰å…¥å·²æ”¹ç‚ºé–‹å ´åŒæ­¥è®€å– optimized `*_low.glb`ï¼Œä¸å†ä½¿ç”¨ 22MB ç´š FBX ä¹Ÿä¸å†å»¶å¾Œåˆ°éŠæˆ²éŽç¨‹ä¸­è¼‰å…¥ï¼›Playwright å·²é©—è­‰ `#start-btn` å¯ä»¥è‡ªå‹•é»žä¸‹é€²å…¥ `mode=playing`ã€‚
- 2026-05-17: 確認玩家頭上灰黑輪廓不是老鼠素材，而是 `scene.background` 天空畫布裡硬畫的 101 剪影；已移除該 tower silhouette。
- 2026-05-17: 神廟門口卡頓修正改為 chunk/場景預熱：初始化時預生成完整 temple corridor(`TEMPLE_A_CHUNK_IDX-2 .. TEMPLE_CHUNK_IDX+4`)，並移除 `startTempleGathering()` / `startTempleAwakening()` 內額外的 `ensureChunksAroundX()`。
- 2026-05-17: 大神像啟動離壇補上 `awakenDropStartDistance`，先在祭壇上站穩/起步，離開祭壇範圍後才開始往地面 lerp 下降；小神像轉化額外提高 `spawnBiteBlood` / `spawnBodyBlood` 噴血量。
- 2026-05-17: 已接入 `asset/sound` 音效系統：主角移動/跳躍/連續啃咬、寄生完成、人類走路/驚慌/死亡、垃圾桶倒地、攤販起火/砸毀、車輛砸毀、大神像腳步、儀式 swarm loop、老鼠群叫聲；腳步類改用節拍播放，不是每幀觸發。
- 2026-05-17: Loading 畫面已改用 `asset/COVER.png` 當封面，`intro-copy` 改成可滾動說明文字；並把 `asset/game frame/` 內的遊戲框、按鈕/地圖框、host life 框、loading 視窗框接成可在 layout object editor 內繼續手調的位置/尺寸內建 frame。
- 2026-05-17: 音效系統新增依玩家位置做距離衰減；主角跳躍改成優先重用已預載 audio 以減少起跳延遲；新生小老鼠音效改用主角移動音效；新增 `asset/sound/人類被咬尖叫/` 依角色類型映射的被咬尖叫聲，啃咬期間會重複觸發。
- 2026-05-17: 重新依 `asset/game frame` 檔名落位：`遊戲畫面裝飾框` 改成包住整個遊戲外框，`按鈕以及地圖的裝飾框` 改成各自套在 minimap / jump / bite / pickup / drop host 外圈，`host life bar` 套在左側宿主血條外框；`loading畫面的視窗` 改綁在 `intro-card` 本體，跟著 loading 視窗一起縮放與移動，不再額外浮在文字層上方。
- 2026-05-17: NPC 人類腳步聲已關閉，只保留玩家目前寄生中的宿主有人類走路/跳躍音效；玩家啃咬時的人類尖叫改成只在啃咬開始時播放一次；街道新增常駐背景音 `街道背景音效.wav`，進入廟宇時會平滑淡出；所有跟隨鼠群移動時會持續播放 `主角移動音效.mp3`。
- 2026-05-17: 女高中生在寄生宿主 idle 狀態下改套用男高中生 idle clip，避免站立時停在 walk pose；Temple idol 資產載入改成初始化階段與人類 FBX 並行預載，在不延後遊戲內載入的前提下降低 loading 總等待時間；loading 說明文字底框已移除並放寬預設寬度，後續可直接在 layout editor 繼續手調全文寬度。
- 2026-05-17: 重新收斂玩家音效規則：老鼠型態只有玩家按住移動時才連續播放 `主角移動音效.mp3`，寄生宿主只有玩家按住移動時才連續播放 `人類走路的聲音...mp3`，驚慌腳步聲映射已移除；寄生完成仍在 `enterHost()` 當下立即播放 `完成寄生...mp3`，街道背景音固定用單一常駐 loop，在進入神廟時淡出。
- 2026-05-17: loading 封面切到壓縮版 `asset/COVER@.png`，所有內建遊戲/按鈕/host life/loading 視窗框與 chaos/rats art 預設路徑都改指向 `asset/game frame/壓縮板圖框/`，並把 UI layout storage key 升到 `urbanLegendMouseUILayoutV5`，強制清掉舊版 frame 路徑快取。
- 2026-05-17: layout admin 的 locked 物件現在不再吃到舞台點擊命中：overlay 會停用 pointer events，direct hit-test 也會跳過 locked target，物件清單列本身也不再把 locked 物件重新選取，避免鎖住後還擋住其他物件。
- 2026-05-17: 音效延遲修正改成預熱 pool：`playerMove / playerJump / humanWalk / screams` 等 one-shot 不再每次 `cloneNode()` 臨時建音源，而是預建多聲道池；其他跟隨小老鼠移動聲改回逐隻節拍播放 `主角移動音效.mp3`，並保留距離衰減。人類尖叫總音量再降 40%，loading 背景同步切到更小的 `asset/COVER2.png`。
- 2026-05-17: 進一步修正音效「播一陣子後失效」的根因：`cooldown` 改成按 `key + source` 個別計時，不再讓整群老鼠或所有破壞音效共用同一個節流；layout editor 也新增 `Rot -15 / Rot +15` 旋轉操作、custom frame 的 copy/paste 真正會複製出新 frame、object list 改成獨立滾動容器以降低 scroll 回彈；`Rat City` 圓框與直式 `Host Life Frame` 預設位置一併補齊。
- 2026-05-17: layout editor æœ¬è¼ªé‡å° object panel äº‹ä»¶å†æ¸…ç†ï¼š`lock/hide/up/down` æ”¹ç”±æŒ‰éˆ•è‡ªå·±è™•ç† clickï¼Œä¸å†ä¾è³´ row bubblingï¼›`UP/DN` çš„ z-order èª¿æ•´æ”¹æˆåªåœ¨ç•¶å‰ scene å…§é‡æŽ’ï¼Œé¿å…è·Ÿ loading/ending å…ƒä»¶äº’ç›¸æ‰“æž¶ï¼›nudge æ”¹ç”¨ stored metrics box è€Œä¸æ˜¯ `getBoundingClientRect()` çµæžœï¼Œé¿å…æ—‹è½‰/transform å¾Œç”¨æ–¹æ¡†åŽ»å›žæŽ¨å°ºå¯¸ï¼Œå°Žè‡´ç®­é ­å¾®èª¿æ™‚äº‚ç¸®æ”¾æˆ–äº‚é£›ã€‚æ‹–æ›³ä¸­ä¹Ÿæ”¹ç‚ºåªåˆ·æ–°ç•¶å‰ overlay / guidesï¼Œä¸å†æ¯å¹€é‡å»ºæ•´å€‹ object panelã€‚
- 2026-05-17: `builtin-frame-gameplay` å·²æ”¹æŽ¥ `asset/game frame/å£“ç¸®æ¿åœ–æ¡†/æ•´é«”åœ–æ¡†.png`ï¼Œåƒ…åœ¨ `scene:'game'` é¡¯ç¤ºï¼›ä¸¦åŠ ä¸Šä¸€æ¬¡æ€§ migrationï¼Œå°‡ `RAT CITY / CHAOS / RATS` æ”¾åˆ°æ•´é«”åœ–æ¡†ä¹‹ä¸Šï¼Œå°‡ minimap / host life / jump / bite / pickup / drop host èª¿åˆ°åœ–æ¡†ä¹‹ä¸‹ï¼Œä¸è¦†è“‹ loading / ending sceneã€‚
- 2026-05-17: loading èˆ‡éŸ³æ•ˆé«”æ„Ÿä¿®æ­£ï¼šè£œä¸Š `primeSFXPlayers()` åœ¨ init éšŽæ®µå…ˆå»ºç«‹/é è¼‰å…¨éƒ¨ audio playersï¼Œ`start-btn` `pointerdown` ç•¶ä¸‹å°± `unlockSFX()`ï¼Œé¿å…é€²éŠæˆ²å¾Œæ‰é–‹å§‹æŠ“ audio è€Œå»¶é²å‡ºè²ã€‚Loading progress æ”¹æˆ `reported + synthetic floor` æ··åˆé¡¯ç¤ºï¼Œéž ready ç‹€æ…‹ä¸‹æœƒä¾ç¶“éŽç§’æ•¸è‡³å°‘æ¯ç§’å¾€ä¸ŠæŽ¨ 1%，ä¸å†é•·æ™‚å¡åœ¨å–®ä¸€æ•¸å­—å¾Œçªç„¶è·³åˆ° 100ã€‚
- 2026-05-18: layout editor å†ä¿®ä¸€è¼ªäº‹ä»¶å±¤ï¼šobject list çš„ `lock/hide/up/down` æŒ‰éˆ•æ”¹ç‚º `pointerup` ç›´æŽ¥è§¸ç™¼ï¼Œä¸å†ç”¨ `click + preventDefault(pointerdown)` çµ„åˆï¼Œé¿å…åœ¨è§¸æŽ§ / ç¸®æ”¾ç‹€æ…‹ä¸‹éœ€è¦é€£æŒ‰å¤šæ¬¡æ‰æœ‰åæ‡‰ã€‚
- 2026-05-18: object list æŒ‰éˆ•äº‹ä»¶å†å¾€å‰æ”¹æˆ `pointerdown` ç•¶ä¸‹ç«‹å³è§¸ç™¼ï¼Œä¸ç­‰ `pointerup/click`ï¼Œé¿å… panel refresh åœ¨æ”¾é–‹æ»‘é¼ å‰å°±æŠŠäº‹ä»¶åƒæŽ‰ã€‚
- 2026-05-18: layout editor æ–°å¢ž stage panï¼šæ”¾å¤§é è¦½å¾Œå¯ç”¨ `Shift + æ»‘é¼ å·¦éµ` åœ¨é»‘è‰²ç©ºç™½å€æ‹–æ›³ `admin-stage`ï¼Œèª¿æ•´ç€è¦½è¦–çª—çš„ä¸Šä¸‹/å·¦å³ä½ç§»ï¼Œä¸å†åªèƒ½å›ºå®šçœ‹ä¸­é–“ã€‚
- 2026-05-18: ä¾ä½¿ç”¨è€…è¦æ±‚ï¼Œé™¤ `æ•´é«”åœ–æ¡†.png` å¤–å…¶é¤˜ PNG è£é£¾æ¡†å…¨éƒ¨é è¨­é—œé–‰ï¼š`chaos/rats art` é è¨­ `src=''` ä¸¦ hiddenï¼Œ`minimap/jump/bite/pickup/drop/host-life/loading` çš„ builtin frame é è¨­éƒ½ hiddenï¼Œexisting layout è¨­å®šä¹Ÿæœƒåœ¨ migration éšŽæ®µè¢«å£“åˆ° hiddenã€‚
- 2026-05-18: layout editor ????????????? bug:`beginUILayoutPointerDrag()` ?????????? text target ????,?? loading card / ending / drop host art / ?? HUD ?????????????????????? layout target ????? resize,?? `setUILayoutSelection()` ???? scene ??,?? loading / ending ?????????????
- 2026-05-18: ?? loading scene ? `intro-progress / intro-progress-text / intro-progress-track` layout target,? loading ??????????? editor ??????????;????????? `intro-card / rats-win` ? min-size ??,??????????????
- 2026-05-18: mobile layout editor ????????? 390x844 ?????????????????? viewport ???,?? resize / media-query change ????? admin-stage ??? overlay??????????? mobile bucket ??????????,????????????
- 2026-05-18: loading progress ????? group target,??? `intro-progress-text` ? `intro-progress-track` ???????????;editor ??? loading / ending / text ???? stored metrics box,?? title / progress ???????????????????
- 2026-05-18: mobile UI drift root cause fixed: `.ui-art-host{position:relative}` was overriding fixed positioning on action buttons / Rat City / Chaos / Rats hosts, so real mobile interpreted saved editor percentages in the wrong coordinate space. Layout application now forces non-skin targets to `position:fixed`, and the old forced `intro-copy.hidden=true` migration was replaced with a one-time restore so loading description text is no longer programmatically hidden.
- 2026-05-18: reverted the human texture tone-lift/no-spawn workaround and added an emergency real-FBX human variant fallback. If the full multi-human asset batch fails, the game now retries a single male-student FBX set instead of leaving `HUMAN_VARIANTS` empty and spawning placeholder pillars.
- 2026-05-18: layout editor exposes the pink loading button and Rat City title as text/style targets, filters all built-in `ring` frame entries out of the object list, preserves Rat City child art while editing its label text, and reapplies the saved loading button label after boot readiness.
- 2026-05-18: human FBX loading is now per-asset tolerant instead of all-or-nothing. Failed optional human assets return `null`, each original human variant is added only when its source FBX exists, missing animation clips fall back to idle, and the console logs skin/idle/walk status for every loaded variant. Local verification loaded all 8 human variants with `skin=true idle=true walk=true`.
- 2026-05-18: Rat City panel was rebuilt around the same loading-card frame art and its controls were moved out of the input row so Mini/Back no longer overlap the title or Save/input row. Ending overlay also now uses the loading-card frame art.
- 2026-05-18: removed remaining startup 404s: rat squeak now aliases the existing player move sound, missing `asset/photos/rat_reference.png` is no longer requested, and missing optional `idol_skin_pre/post.jpg` files are not fetched. Playwright 404 pass now reports only successful human variant logs.
- 2026-05-18: ending layout cleanup now hides gameplay custom frames during `body.game-won`, restores empty saved ending text defaults, widens the ending title/subtitle/button positions, and brightens the ending subtitle so the card no longer overlaps its own text.
- 2026-05-18: restored human NPC model loading from the 12-hours-prior commit `1088a5f`: removed the later `addLoadedHumanVariant` / emergency fallback path, reinstated direct `HUMAN_VARIANTS.push(makeHumanVariant(...))` setup for the 8 original human variants, restored primary `characterSource` / `HUMAN_SKIN` / `HUMAN_CLIPS` assignment, and put Hua-Hua auntie back on her own walk/run FBX instead of the middle-aged-woman clips.
- 2026-05-18: hardened human NPC loading for device/deploy failures: human FBX/texture assets now load per-asset instead of all-or-nothing, each of the 8 configured human variants is kept when its source FBX loads, missing optional clips/textures fall back without killing the whole batch, and empty human variant state skips NPC spawns instead of creating black placeholder pillars. Also preloads SFX before BEGIN is enabled, fixes admin-stage layout application for loading/ending target resize boxes, hides bite hints outside gameplay, changes Rat City mini/back labels to compact icons, adds neon glow to Chaos/Rats art, and makes the joystick stick default/editable pink.
- 2026-05-18: layout editor drag math now uses the real on-screen rect instead of a synthetic box and no longer divides pointer movement by admin zoom, fixing jumpy move/resize and the mismatch where editor overlays drifted away from the real mobile layout. Intro/start audio warm-up is now awaited before entering gameplay, and asset load timeout was raised to 60s so slower mobile devices do not drop human NPC spawns during boot.
- 2026-05-18: fixed the new startup deadlock/perf regression: BEGIN no longer waits on warming every audio instance, SFX boot preload is limited to a small critical set, audio polyphony/pool creation is now lazy instead of spawning a huge pool at boot, and startup chunk residency was cut down from the giant full-ring preload to a small gameplay window plus the immediate temple-adjacent chunks. This removes the frequent “loading 100% then BEGIN hangs” case and substantially reduces in-game scene weight. Human NPC walk SFX remains disabled for non-possessed NPCs; only the player-controlled host still uses `humanWalk`.
- 2026-05-18: restored strict intro-time preload per request while keeping BEGIN non-blocking: all SFX files are now preloaded before the start button is enabled, but the click itself no longer waits on audio warm-up promises. Full temple/street chunk range is again pre-generated during intro with frame-yielding to avoid a hard freeze, the unused viewport edge DOM nodes were removed, and temple awakening swarm rats now carry their own plaza snapshot so the scripted altar-climb path no longer fights the generic rat temple-bound AI. Awakening now seeds swarm bursts for both temple idols (primary + decorative twin), targeting each doorway/altar independently.
- 2026-05-18: restored the temple awakening burst to immediate old-style spawning instead of queued `pendingSwarmBursts`: `startTempleAwakening()` now instantly spawns 300 swarm rats per active temple plaza, deducts the matching virtual rat count, and leaves the queue counters cleared. Also restored missing prop/fire SFX hooks (`stallFire` on flame spawn, explicit throw/kick break sounds for stall/bin/vehicle props), boosted follower-rat move SFX audibility, and removed the old builtin ring frame definitions from desktop/mobile default custom-frame payloads plus the old frame layer migration map. Smoke test on local server: `BEGIN` unlocked at 12s, entered `mode=playing`, no page errors, 8 human variants loaded successfully.
- 2026-05-18: fixed the post-parasite audio fade bug at the root: distance attenuation now uses the current player-controlled actor (`game.controlled` / host) as the listener instead of always using the original rat body, so host footsteps / break SFX no longer fade out after the host walks away from the hidden rat body. Temple plaza snapshots now preserve `rearDoorwayPos/rearStreetPos`, and the restored legacy awakening flow now spawns exactly 300 ritual rats for the player's current temple only, split across that temple's front/rear entrances instead of spawning across both temples. Mobile layout buckets now persist the real mobile `visualViewport` size into the saved config so desktop-side mobile editing can preview against the same dimensions the phone used. Human NPCs now share the male-student action clip set, clips are filtered against each source rig to avoid `PropertyBinding` missing-bone spam, distant NPC humans are temporarily de-simulated/de-rendered to cut the full-preload frame cost, and the middle-aged male / middle-aged female / male-student base-texture paths were switched to their exact `*_BaseColor.jpg` files for more reliable skin loading on case-sensitive deploys. Local smoke test still enters gameplay cleanly with no page errors.
- 2026-05-19: mobile layout editor drift was finished at the stage-application layer: game HUD targets are again applied in stage-local percent space during admin mode, so desktop-side `MOBILE` preview now matches the saved phone layout instead of scattering buttons/art outside the gameplay frame. Verified with Playwright + screenshot (`artifacts/mobile-editor-check.png`) that jump/bite/pickup/drop/chaos/rats/joystick all stay inside the mobile frame.
- 2026-05-19: temple ritual flow was hardened into a single current-temple path. `getCurrentTempleSwarmTarget()` now resolves the player's actual temple zone (primary or decorative twin), promotes that idol if needed, and `startTempleAwakeningLegacy()` immediately spawns 300 ritual rats for that one temple only. Non-ritual rats inside the temple are forced back out toward the nearest street entrance, and generic rat spawning no longer marks new spill/breeding rats as `templeBound` once the ritual has already started. Debug text output now includes concise temple state (`inside/gathering/started/phase/swarmVisuals`) for future verification. Playwright verification: after `Shift+T` + `Digit1` and moving into the temple, `render_game_to_text()` reported `temple.started=true`, `phase='gather'`, `swarmVisuals=300` immediately on temple entry.
- 2026-05-19: fixed the rear-door ritual escape case and editor drag instability. Temple loop portals now ignore `templeSwarmVisual` rats so the rear-door half of the 300-rat burst no longer gets teleported out of the temple, and a visual-rat budget pass now keeps `game.rats.length <= 500` by collapsing far non-critical rats back into `virtualRats` without touching ritual swarm visuals. Human skin loading was hardened by treating normal/roughness/metal maps as non-color textures, correcting the schoolgirl base-color path to the explicit `...BaseColor.jpg`, and adding a lightweight periodic skin repair pass for nearby humans if a mesh loses its map and turns gray. Layout editor pair-snapping is now restricted to the old viewport helper frames only, and editor measurements for normal HUD objects / skin-art parents use stored layout rects instead of live overflow-influenced DOM bounds, which stops `Chaos/Rats/Host Life` dragging from snapping each other, jumping, or resizing unexpectedly. Verification: Playwright drag test moved `host-life-box` while preserving width/height exactly, and a ritual smoke test reached `temple.started=true`, `swarmVisuals=300`, `visualRats=481`.
- 2026-05-20: Removed the remaining human debug pose-reset path. `debugValidateHuman()` no longer calls `forceHumanActionPose(h,'walk')` after a below-ground correction, so a transient ground clamp can no longer restart the clip from frame 0 and reintroduce flicker / T-pose flashes.
- 2026-05-20: Fixed another static-rat source in breeding. `tickBreeding()` now respects the visible-rat cap up front and every visible bred rat sets `followPlayer=true`, matching the spill-rat path so street rats do not spawn as frozen non-following sprites.
- 2026-05-20: Split loading `BEGIN` into two independently editable layout targets: `start-btn-label` for the text and `start-btn` for the button body. This also restores normal size editing on the button itself instead of treating the button as a text-only target.
- 2026-05-20: Loading background image `asset/COVER2.png` is no longer used by `#intro`; intro now shows the 3D scene with a slow rotating street showcase camera pass. Street ambience is also kicked immediately in `startGame()` once audio is unlocked so gameplay no longer enters with a silent delay.
- 2026-05-20: `+CHAOS` comic text was enlarged substantially (`fontSize:96`, `scaleX:4.9`, `scaleY:1.7`) so destruction feedback reads closer to stall-width on screen.
- 2026-05-20: Layout editor corner-resize now preserves aspect ratio only for custom frames and skin art. Normal HUD/layout targets (for example `chaos-box` / `rats-box`) now resize freely instead of "jumping" because a forced proportional scale was being applied to generic UI boxes.
- 2026-05-20: Human T-pose regression path traced to `makeHumanVariant()` bypassing `getSourceCompatibleClip()` and feeding raw shared clips into mismatched NPC skeletons. Restored per-source filtered clip binding so variant rigs only receive tracks for bones they actually contain, matching the console `PropertyBinding` spam root cause the user reported.
- 2026-05-20: Restored visible prop flip-over motion instead of instantaneous "teleport to fallen pose." `triggerPropTip()` now creates a short cubic eased tip animation and `tickProps()` advances it over time; cleaner restoration also clears the new `tipAnim` state.
- 2026-05-20: Host kick hit detection was widened and moved slightly closer to the host body (`attackOrigin` 0.82 m, looser front-dot / radius checks) so stalls and nearby humans react more reliably when attacked.
- 2026-05-20: Removed the remaining skyline/101 opening backdrop by stripping the baked silhouette from `scene.background`, and removed the loading/ending pseudo-element frame art from `.intro-card::before` and `#rats-win::before` so those scenes no longer display the gameplay-style frame.
- 2026-05-20: Layout editor stability pass for `builtin-frame-gameplay`: during admin mode all layout targets, including custom frames, now measure from stored layout boxes instead of volatile live DOM rects, and custom frames no longer force aspect-ratio corner resize. This is intended to stop gameplay frame position jumping while resizing one edge outward into the black workspace.
- 2026-05-20: Corrected the mistaken loading/ending frame removal. Restored the loading card / ending card frame art, and instead hid `gameplay` custom frames during `body.intro-active` so the gameplay frame no longer overlays the loading screen.
- 2026-05-20: Added opaque-pixel bounds measurement for custom-frame images. The layout editor now shrinks custom-frame selection boxes to the visible non-transparent portion of the PNG, and converts drag/resize rects back to full stored metrics so gameplay-frame handles better match the actually visible frame art.
- 2026-05-20: Host kick physics chain was simplified back to a true forward tip. `doHostAttack()` no longer mixes "tip now" with an immediate fake throw impulse, `triggerPropTip()` now uses a longer eased fall with direction-flipped rotation so objects fall away from the attacker, and generic destroyed street props now emit at least a small rat burst even when `spawnRatsOnTip` was previously zero.
- 2026-05-20: Desktop human flicker mitigation pass: far human LOD hide/show was disabled on non-mobile builds, and animation state switches no longer `stop()` non-selected actions each transition. This is intended to eliminate the desktop-only bind-pose flash / visibility strobe path still reported after the earlier clip-filter fix.
- 2026-05-20: Fixed the prop-fall coordinate mismatch at the source. `triggerPropTip()` now reads gameplay yaw the same way movement/throw code does (`sin` for X, `cos` for Z), so host-kicked props fall forward instead of sideways/backward, and `doHostAttack()` now picks the single best front prop before tipping it instead of half-missing large vehicle/stall AABBs.
- 2026-05-20: Street burst rats now reserve visible slots before spawning. `summonSpillRats()` first collapses far non-critical rats back into `virtualRats` when needed, then spawns the new burst rats with their scatter/follow state intact, so breaking street props still produces visible rats even near the visual cap.
- 2026-05-20: Civilian human clip binding was tightened further: shared FBX `.position` tracks are stripped because gameplay already drives locomotion and the shared hip translation was still making variants dip underground, cloned skinned meshes rebuild fresh skeleton inverses, and all cloned skinned meshes disable frustum culling to reduce bind-pose / disappear flicker.
- 2026-05-20: Layout admin no longer hides gameplay custom frames while the editor is open, and `builtin-frame-gameplay` now uses a fixed known visible-bounds map instead of depending on runtime PNG sampling. This is meant to bring the gameplay-frame selection box back onto the visible frame art.
- 2026-05-20: Immediate rollback on the last human-rig regression: restored shared clip position tracks and restored cloned skeleton binding to use the source `boneInverses` instead of recalculating them. The previous change made the reported human sink/T-pose regression worse instead of better.
- 2026-05-20: Fixed a hard temple-loop streaming bug for host possession. Loop-portal teleports and periodic chunk prefetch now follow `game.controlled` / `parasitisedHost`, not just the original rat body, so possessing a human no longer crosses the temple rear portal into an unloaded blank section.
- 2026-05-20: Street prop rat bursts now spawn from behind the player on host kicks, run toward the broken prop, then scatter and fall back into follower behaviour. Also excluded `followStatue` ritual rats from spill-rat visual eviction so awakened-procession rats are not accidentally removed by later street chaos events.
- 2026-05-20: `+CHAOS` comic text was hardened against the recent visibility regression: raised size again, disabled depth test so props cannot hide it, and tagged it as `chaos-comic` for easier future debugging.
- 2026-05-20: Continuing debug for reported regressions: human NPC slow/idle/obstacle response, sparse start-area humans, gameplay stutter, idol procession T-pose/stuck, missing escort rats, and ritual rat floor height. Initial inspection found human mixer timeScale is tied to low wander speed (~0.45-0.6x), group size can be 3-7 instead of 3-5, start area relies on sparse chunk spawns, and post-awakening followStatue rats are all full critical sims.
- 2026-05-20: Fixed the reported NPC/procession regressions in `index.html`: seeded visible start-area humans in 3-5 person groups, decoupled non-player human animation timeScale from slow wander speed, added explicit idle dwell and obstacle turn-away, raised ritual rat ground offset, restored temple idol walk action weight for aliased idle/walk/run clips, nudged stuck idol procession waypoints, kept 54 mobile / 96 desktop escort rats while limiting active hunters, and made followStatue hunters ignore blocker collisions so they can reach/climb humans. Also reduced startup stutter by culling seam-preview clone groups away from seams and hiding resident-ring chunks/props outside view instead of rendering the full preloaded ring. Final Playwright mobile smoke: start nearHumans=20 with groupSizes `[5,4,4,4]`, idlingNear=15, procession followStatue=54, climbingHuman=14, lowRitual=0/minY=0.41, idol walk effectiveWeight=1, chaos=0, no page errors.
- 2026-05-21: Debug pass for `index.html` audio/NPC/ritual/seam issues. Bound requested SFX files for stall/vehicle break, glass/small-table break, and parasite completion; generic breakable props now route through one immediate `triggerPropTip()` path with the requested break SFX. Doubled resident/start-area human population, kept local visible seeding at 3-5 person groups, and added per-NPC walk/idle/turn phase bias so grouped humans no longer synchronize. Host/parasitised kick animation now bypasses slow locomotion timeScale (`kickScale=1.12` in Playwright), while non-player idle/walk timing stays readable. Ring seam wrapping now runs before world collision resolution and sets a short portal lock so the temple idol cannot collide against the seam on the teleport frame. Procession rats were slowed from `baseSpd*5.2` to `baseSpd*2.6`, ritual visible Y offset lowered from `0.40` to `0.20`, and clamped to avoid floor sinking. Verification: `node --check` passed; mobile Playwright start reported no page errors, `nearHumans=40`, group sizes `[5,4,4,3,3,3,3,2,2]`, `idlingNear=28`; full interaction diagnostic reported `followStatue=54`, `maxRatSpeed=3.12`, `minRitualY=0.21`, `lowRitual=0`, seam wrap shift `-524`, and idol stuck timer `0`.
- 2026-05-21: Deep stability/performance pass without reducing NPC or rat counts. Idol ring wrapping now manually cleans the old `_ringMirrorRoot`, skips collision during the portal-lock frame, logs `Idol Warp: From [X1] To [X2], Proxy cleanup: [Status]`, and restarts the idol mixer via `stopAllAction()` + replay of the walk action so the post-warp entity cannot remain in a frozen proxy pose. Dynamic ring mirrors now copy matching bone transforms from the live actor into the proxy each frame; the verified idol mirror rebuilt after lock with `bonePairs=33`. Ritual/followStatue rats now clamp per-frame XZ displacement (`RITUAL_RAT_MAX_FRAME_MOVE`) and cap procession speed at `RITUAL_RAT_MAX_SPEED=3.4`; chaos=1 diagnostic dropped followStatue `maxStep` to `0.0567` and `maxCurSpeed` to `3.4`. Rat target-to-climb transition now stores climb start coordinates and blends onto the human over `0.22s` instead of snapping. Mobile performance tuning: distant non-critical skinned humans use distance-based `AnimationMixer` LOD updates, followStatue rat brain/world probes back off by distance, and rat sprite animation is no longer double-updated in both `tickFollowerRats()` and the main loop. Verification: `node --check` passed; robust mobile Playwright gameplay diagnostic had no page errors, emitted the Idol Warp log, reported `walkWeight=1`, `stuck=0`, mirror visible after lock at the opposite side, `near=43` humans with group sizes `[5,4,4,3,3,3,3,3,3,2]`, and `lodHumans=30`. Standard develop-web-game client also ran and screenshot was inspected, though it remains on intro because that generic client does not wait for BEGIN unlock.
- 2026-05-21: Follow-up handover fix for the user's `Proxy cleanup: none` report. Temple idols now get stable `idolInstanceId` values and ring mirror roots are tagged with `ringMirrorOwnerId`; `clearRingMirrorForEntity()` now searches the scene by owner id and returns `removed-direct`, `registry-N`, `detached-direct`, or `not-found` instead of the ambiguous `none`. Warp now calls `calculateNewPathIndex(idol.root.position)` after coordinate shift, logs `PathIndex: old->new`, and dispatches `idol-refreshed`; the event handler immediately rewires all followStatue / templeBound / templeSwarmVisual / targetHuman / climbingHuman rats to `rat.targetIdol=currentIdol` and refreshes their target positions. `ensureRitualEscortRats()` and `spawnTempleSwarmRat()` also set `targetIdol` on creation, and followStatue logic now prefers `rat.targetIdol` over the global idol when valid. Idol walking SFX has a 1000 ms throttle in `tickFootstepSFX()` for `idolStep`, reducing the audible step frequency by about 50%. Verification: `node --check` passed; dedicated mobile Playwright handover test first created a visible old-side mirror, then warped the idol and captured `Idol Warp: From 251.25 To -272.75, Proxy cleanup: removed-direct; Mixer: replayed:walk; PathIndex: 0->0; Rat handover: dispatched`; after warp, `mirrorExists=false`, `walkWeight=1`, `walkRunning=true`, and all 54 followStatue rats had `targetIdol===idol`.
- 2026-05-21: Second deep debug pass for seamless idol/ritual/mobile stability. Added explicit `realignIdolPath()` and exposed seam/ritual debug hooks; idol warp now calls the realignment helper after proxy cleanup/mixer replay and still dispatches `idol-refreshed`. Reworked the 300-rat ritual into a 10 second `ritualElapsed / RITUAL_IDOL_CLIMB_DURATION` climb with precomputed per-rat randomized offsets instead of the synchronized orbit/figure-eight path; drop phase now moves rats to the altar/offering table and then dispatches `ritual-finished`, which clears temple targets and switches rats to `followStatue`/`targetIdol`. Removed the old escort downsample in `ensureRitualEscortRats()` so a completed 300-rat ritual remains 300 followStatue rats on desktop and mobile; performance relies on existing rat LOD/brain/world-probe throttles instead of deleting rats. Audio now has mobile one-shot priority gating, per-human bite scream cooldown, high-priority jump/parasite sounds, and host kick end transitions force a 0.045s blend back to locomotion to reduce post-kick drift. Verification: `node --check` passed; `git diff --check` only reported the repo's CRLF warning; develop-web-game client screenshot inspected; dedicated desktop Playwright regression spawned 300 rats, reported progress5=0.50, progress10=1.0, finished=true, followCount=300, and warp cleanup `removed-direct` with target handover true; iPhone UA/viewport regression also finished with followCount=300 and no page errors.
- 2026-05-21: Follow-up for user-reported idol prop blocking / seam mismatch / missing rats and audio. Idol collision now treats props as kickable debris: `idolClearBlockingProp()` tips/pushes blocking props and `resolveWorldCollisions()` skips prop pushback for temple idols, so small ground objects cannot pin the procession. `summonSpillRats()` now detects awakened idol procession and spawns visible `followStatue` rats pointed at `targetIdol` instead of converting destruction rats to virtual counts; idol and host/player prop destruction both create new visible idol followers, and rat spawn/swarm SFX fires on spill, breeding, `addRatSwarm()`, and the 300-rat ritual burst. SFX handling was hardened for mobile: critical keys (`playerBiteLoop`, `playerJump`, `parasiteComplete`, `streetAmbience`, rat swarm/footstep, `ritualSwarm`) are fully unlocked first, the one-shot mobile pool limit was raised to 12 with high-priority preemption, and loop/one-shot `play()` calls retry once after browser decode/autoplay hiccups. Rat swarm movement audio on mobile now responds to nearby moving `followStatue` escorts, not only player movement. Seam preview continuity was restored after idol rig swap by rebuilding preview clones after `removeRingSeamPreviewGroups()`, expanding preview chunks 5->8, and holding both seam preview groups visible for ~0.72s after player/host wrap. Verification: `node --check` passed; desktop Playwright regression confirmed prop clear distance 1.37m, idol break followStatue 300->304, host break 304->314, new rats `targetIdol===idol`, seam preview hold active with both groups visible, and key SFX active; iPhone viewport regression confirmed idol break spawned +2 followStatue rats and `playerJump`/`parasiteComplete`/`ratSwarmLoop` plus `playerBiteLoop`/`streetAmbience` were active with no page errors.
- 2026-05-21: Fixed the latest seam-state/audio/frame issues. Prop destruction now syncs to the ring twin via `findRingTwinProp()` / `syncRingTwinPropTip()`, and seam preview prop clones are hidden/restored with the live prop state so walking back across the loop no longer shows an already-destroyed prop as intact. `idol-refreshed` now carries `shifted` into the rat handover path; all followStatue escorts are detached from old human targets, shifted by the same warp delta, retargeted to the current idol, and kept visible, while follow-rat movement uses `nearestRingDelta()`/`ringShiftNearestToReference()` so escorts take the short seam path instead of walking around the full ring. Pre-ritual prop breaking with 3+ nearby rats now plays the `鼠群音效.MP3` burst twice with a stagger, and normal rat-spawn SFX uses the same layered swarm burst for larger groups. Street ambience now uses two preloaded/crossfaded `streetAmbience` layers (`streetAmbience` + `streetAmbienceB`) with longer audio preload timeout before BEGIN, reducing start/end loop gaps and mobile first-play misses. Desktop `builtin-frame-gameplay` now points to `asset/game frame/壓縮板圖框/電腦版遊戲畫面裝飾框.png`, matching the provided aspect frame. Verification: extracted module `node --check` passed; develop-web-game client ran and screenshot was inspected; desktop Playwright diagnostic reported twin prop tipped=true, two active ratSwarmLoop one-shots, both street ambience layers playing, and 8/8 escort rats shifted by the idol-refresh delta with no page errors; iPhone viewport diagnostic reported both street ambience layers active, two ratSwarmLoop one-shots, and 5/5 escort rats shifted with no page errors.
- 2026-05-21: Focused mobile stutter + audio-system pass. Replaced scattered SFX pools with one centralized Web Audio AudioManager in index.html: one AudioContext, manifest-driven preload/decode during loading, group/master gains, unlock ping, loop state, one-shot playback, queue-before-unlock handling, and debug state. Bound unlock attempts to start button pointerdown/touchstart/click, joystick/action/canvas pointerdown, and document touchstart; failed unlock attempts now clear the promise so later real gestures can retry. All 23 current SFX buffers preload/decode before BEGIN, with per-file warnings instead of boot failure. Street ambience and rat swarm loops now use persistent Web Audio loops and gain ramps rather than repeated start/stop; mobile follower-rat audio probing uses a throttled grid query instead of scanning all rats every frame. Added Shift+P / ?debugAudio=1 audio+perf overlay updating once per second. Mobile render pass now uses a smaller 426x240 internal RT and a lighter single-sample VHS shader while preserving scene density; renderer.info is manually reset so debug draw calls/triangles report both scene and screen pass. Verification: module extraction node --check passed; develop-web-game client ran and screenshot was inspected (generic client still clicks before long loading enables BEGIN); custom desktop and iPhone Playwright diagnostics waited for loading, clicked BEGIN, verified AudioContext=running, unlocked=true, ready=true, loadedBuffers=23, failedBuffers=0, queued=0, two active crossfade street ambience loops, player jump / parasite / rat swarm one-shots, mobileAudioMode true on iPhone, and no page errors.
- 2026-05-21: Mobile-only audio silence debug. Root suspect was Safari/WebKit compatibility in the new Web Audio path: `AudioNode.connect()` chaining can return undefined on iOS even though desktop Chrome returns the destination node. Replaced all `src.connect(gain).connect(...)` usage with explicit `connectThroughGain()` steps, added AudioContext constructor fallback when iOS rejects options, starts the unlock ping synchronously before awaiting `resume()`, gives `resume()` a short timeout so touchend/click can retry instead of being locked behind a pending promise, added AudioContext `statechange` queue flush, added touchend/pointerdown/click unlock retries, and sets `navigator.audioSession.type='playback'` when available. Verification: module extraction `node --check` passed; develop-web-game client ran and screenshot was inspected; custom iPhone Playwright test with simulated Safari `connect()` returning undefined and simulated constructor-options rejection still reached `AudioContext=running`, `unlocked=true`, `loadedBuffers=23`, `failedBuffers=0`, `queued=0`, active playerJump/parasiteComplete/ratSwarmLoop one-shots, two streetAmbience loops, and no page errors.
- 2026-05-21: Layout/editor naming pass only; audio and mobile performance logic intentionally untouched. `index.html` title/share meta/loading title now say `URBAN LEGEND RAT`. Layout Admin viewport math now uses the scaled admin-stage rect, desktop stage matches the real viewport aspect, layout-controlled elements and overlay boxes use border-box sizing, and skin-art bounds read the live parent rect so `chaos/rats box` and `chaos/rats art` overlays match actual DOM before and after zoom. Dragging is freeform by default, Ctrl/Cmd-drag snaps, Arrow nudges 1px, Alt+Arrow 0.5px, Shift+Arrow 10px. Begin button is force-migrated editable/unlocked once, supports selectable styles (`default/neon/glass/sigil/slab`), and no longer has the old 18px editor minimum. `RAT CITY` label now has neon glow. Verification: module `node --check` passed; `git diff --check` only reported CRLF warning; develop-web-game client screenshot inspected; custom Playwright Layout Admin test confirmed title=`URBAN LEGEND RAT`, `chaos-box/rats-box/chaos-box-art/rats-box-art/rat-city-btn` overlay-vs-target diffs ~= 0 before and after zoom, Begin style select applies `neon`, ArrowDown moves about 1px, and no page errors.
- 2026-05-21: Focused UI/gameplay follow-up in `index.html` without touching the stabilized mobile audio/perf architecture. Ending screen now uses the same layout config at runtime, admin preview no longer overrides its background/z-index, and `body.game-won` forcibly hides the gameplay frame. Begin button CSS padding/min-size was removed so it can be flattened in Layout Admin. `RAT CITY` now has a flickering neon label/halo, buttons gain a pressed color state, and `+CHAOS` text starts lower. Street ambience distance source now follows the controlled host instead of the hidden rat. Added Shift+V visual filter cycling (`vhs`, `dreamcore`, `vhs-dreamcore`) plus +/- intensity. Host attack input now distinguishes tap=punch, repeated tap=punch combo, hold=kick, loading `asset/ACTION/player_punch.glb` and `player_punchcombo.glb`. Vending machines (`vending-machine`) and power poles (`power-pole`) are registered as destructible props and route break SFX through `vehicleBreak`; stall fire SFX path remains active via existing `stallFire` calls. Verification: extracted module `node --check` passed; develop-web-game client ran with a wait-only action and screenshot inspected; custom Playwright diagnostic confirmed filter state changed to `dreamcore` strength `1.1`, ending screen display `flex` with gameplay frame `none`, `vending=122`, `pole=226`, and `punch/punchcombo` actions present on 130 humans, with no serious console/page errors.
- 2026-05-21: Follow-up UI/animation debug for the reported punch/combo upside-down pose and WYSIWYG mismatches. `punch` and `punchcombo` now strip Mixamo Hips/root rotation tracks after clone-root filtering/position locking so the host stays upright while the arm/torso attack keys still play. Default visual filter is now `vhs-dreamcore` at max strength `2`. Rat City neon is fixed at the DOM source: `setRatCityMode()` no longer destroys the `.ui-art-label`/art children with `textContent`, and the label has a stronger visible flicker. Rat City/result mode hides HUD/minimap/gameplay frame and uses the Ending Card metrics for its panel sizing; real `triggerRatsWin()` also forcibly exits Rat City and hides the gameplay frame. Layout text editing now uses textareas for ending/loading text fields and applies `white-space: pre-wrap`, preserving spaces/newlines in runtime. Verification: module `node --check` passed, `git diff --check` only reported the repo CRLF warning, develop-web-game client screenshot inspected, and custom Playwright diagnostics confirmed default filter `{mode:"vhs-dreamcore", strength:2}`, Rat City label animation `ratCityNeonFlicker`, gameplay frame hidden in Rat City and ending, ending text `pre-wrap`, all ending text inputs are `TEXTAREA`, and punch/combo clips have no Hips rotation tracks with no serious console/page errors.
- 2026-05-22: Added Cloudflare Workers + D1 online Rat City leaderboard with minimal gameplay changes. New files: `worker/leaderboard.mjs`, `worker/schema.sql`, `worker/README.md`, and root `wrangler.toml`. `index.html` now keeps localStorage fallback, normalizes names/scores, shows top 20, fetches online rankings only when Rat City opens or debug forces refresh, and posts scores asynchronously after local save. No audio/scene/NPC/gameplay logic was changed. Verification: Worker `node --check` passed; extracted `index.html` module `node --check` passed; mock D1 Worker test passed for GET/POST/CORS/score validation/pruning; Playwright frontend test passed for online POST, online refresh, and API-failure localStorage fallback; develop-web-game client ran and screenshot was inspected.
- 2026-05-22: Deployed the online leaderboard to Cloudflare. Logged in via Wrangler OAuth for `zhenggdove@gmail.com`, created D1 `urban_legend_rat_leaderboard` in APAC (`f45b0f66-b766-4914-b996-ba2757b09123`), applied `worker/schema.sql`, registered workers.dev subdomain `zhenggdove-artist.workers.dev`, and deployed `urban-legend-rat-leaderboard` at `https://urban-legend-rat-leaderboard.zhenggdove-artist.workers.dev`. Updated `index.html` `RAT_CITY_LEADERBOARD_API_URL` to `/api/leaderboard`, and set `workers_dev=true` / `preview_urls=false` in `wrangler.toml`. Live HTTPS API verification passed for health, POST, GET, invalid score rejection, and disallowed Origin rejection; temporary CODEX test rows were deleted afterward. Local frontend fallback still passes when CORS blocks localhost. Note: on this Windows environment, IPv6 TLS to workers.dev failed, but IPv4 HTTPS succeeded; browsers should normally fallback automatically.
- 2026-05-22: Fixed mobile WYSIWYG drift for the right-side `chaos-box` / `rats-box` only. Root cause was the mobile media query forcing `min-width:144px`, while the desktop Layout Admin mobile preview did not apply that media query, so live mobile stretched the boxes after editing. `applyUILayoutMetrics()` now clears inline `minWidth/minHeight` for those two HUD boxes so saved mobile layout metrics fully control their real size. Verification: extracted `index.html` module `node --check` passed; iPhone-size Playwright check confirmed real box center/width/height match saved mobile metrics within ~0.02px and computed `min-width=0px`; develop-web-game smoke ran and screenshot was inspected.
- 2026-05-24: Rat City leaderboard mobile/input fix in `index.html`. Added explicit candidate-name validation so `Save` stays disabled until the player types a non-placeholder name, and `Rat King Name:` can no longer be saved as an entry. Also added mobile-friendly focus handling (`touch-action: manipulation`, input/change/focus/click/touchend hooks) so tapping the name field is more likely to open the keyboard on phones. Verification: extracted module script `node --input-type=module --check` passed. No browser-side Playwright/mobile repro was run in this pass.
- 2026-05-24: Continued the interrupted verification for the Rat City mobile input fix. Restarted local `python -m http.server 8123` and ran a dedicated iPhone-size Playwright probe against the forced-open Rat City panel. Browser-side checks confirmed placeholder text `Rat King Name:` and empty input both drive `Save` disabled, valid typed names enable `Save`, computed `touch-action` on `#rat-city-name` is `manipulation`, and the input's synthetic `touchend` / `click` focus hooks both move focus onto the field. A normal Playwright tap reported intermittent pointer interception by overlapping rendered text during the forced-open harness, but the field still ended focused; screenshot saved to `test-results/rat-city-mobile-check.png`. Because this harness bypassed the real gameplay/win flow and module-scoped `setRatCityMode()`, it should not be treated as a full end-to-end gameplay-open regression test.
- 2026-05-24: Fixed the reported mobile Rat City / ending controls in `index.html`. Non-interactive Rat City text layers (`title/desc/score/list`) now use `pointer-events:none`, while the input/buttons keep `pointer-events:auto`, so the mobile name field can receive real taps even when exported ending layout boxes overlap. The name input now focuses on pointerdown/touchstart/click/touchend without preventing the native keyboard path, and the global double-tap zoom blocker skips editable fields. Canvas now tracks two active touch pointers and maps pinch distance changes to gameplay camera distance or Rat City zoom while still blocking browser page zoom / double-tap zoom. The ending `BACK TO THE STREET` Rat City button now uses flex centering and zero vertical padding, including the game-won/admin-ending display overrides, so the label is centered inside the neon frame. Verification: extracted module `node --input-type=module --check` passed; dedicated iPhone Playwright check confirmed real tap focus on `#rat-city-name`, hit target `rat-city-name`, typed value `MobileTester`, Save enabled with `aria-disabled=false`, button display `flex` with label center delta `0,0`, and pinch spread changed `ratCityZoom 26 -> 10`; screenshot saved to `test-results/mobile-rat-city-fixed.png`. develop-web-game smoke ran and screenshot was inspected; only expected localhost Cloudflare leaderboard CORS fallback appeared.
- 2026-05-22: Gameplay fix pass for stall soup / post-temple prop feedback / building signs. Soup/broth FX now ground-locks to `Y=0.035` for soup pots and stalls instead of inheriting the raised pot/table height; visible spill meshes update their local Y against the prop root each tick so they stay near the ground without sinking. Prop break feedback is centralized in `playPropImpactFeedback()`, and host attacks on already-tipped/synced props now still emit a `CLANG!` comic hit, debris, and SFX instead of feeling silent. Host punches/kicks toward building-side facades with no prop/human hit now spawn a falling `falling-sign` prop from the wall, using existing sign textures and break SFX, then dropping toward `Y=0.08`. Verification: extracted module `node --check` passed; targeted Playwright check confirmed a raised soup pot had broth/spill Y `0.035` and a spawned sign had target endY `0.08`; develop-web-game smoke ran and screenshot was inspected.
- 2026-05-22: Fixed the live Cloudflare/D1 Rat City leaderboard write failure. Root cause was a contract mismatch from the earlier D1 schema: frontend submits cumulative `CHAOS` as both `score` and `chaos`, but Worker validation and the live D1 `CHECK` constraint still capped `chaos` at 100, so normal endgame scores returned `400 chaos must be an integer from 0 to 100` and fell back to local-only scores. Updated Worker validation and `worker/schema.sql` to allow `chaos <= MAX_SCORE`, added `worker/migrate-chaos-score.sql` to rebuild existing D1 tables with the wider constraint, applied it remotely to `urban_legend_rat_leaderboard`, and redeployed Worker version `95295ffc-1f1c-44e5-8e17-4a19b8942a12`. Verification: local Worker mock accepted `score=1000, chaos=1000`; live POST from `https://zhenggdove-artist.github.io` accepted `score=1234, chaos=1234` and subsequent live GET returned it; disallowed Origin still returned 403; temporary CODEX rows were deleted and D1 ended with the original 3 real rows.
- 2026-05-22: Reworked the Rat City ending leaderboard layout and Layout Admin export guide. Ending leaderboard now uses the loading-card art as an aspect-preserved background, caps the card inside desktop/mobile viewports, shows 5 rankings per page with a cycling `NEXT PAGE` button across the top 20, shortens mobile score text to prevent overlap, and keeps the post-save button label compact (`ONLINE` / `LOCAL`). Added `rat-city-pager` to layout targets/defaults and a `__ratCityLeaderboardCardV10` migration so existing saved layouts are corrected. Rewrote `LAYOUT_EXPORT_REPLACE_GUIDE.md` to explain editor version-difference warnings and the anchor-based safe replacement flow. Verification: extracted module `node --check` passed; `git diff --check` only reported CRLF normalization warnings; static Playwright DOM/CSS checks confirmed the mobile card stays within 390x844, shows 5 rows, and advances to rows 6-10.
- 2026-05-23: Fixed Layout Admin export and ending-card edit stability. `Export` now still copies `UI_LAYOUT_DEFAULTS`, but also downloads a patched full `index.layout-applied.html`, avoiding manual saves from stale editor buffers. Drag/resize measurement in admin mode now uses stored layout boxes instead of volatile live DOM rects, so text/button targets such as Ending Save no longer change size while being moved. Added `__ratCityEndingCardV12` defaults/migration: desktop ending card is taller, mobile card is taller with tighter score/pager/list text, and the card is unlocked for direct editing. Verification: extracted module `node --check` passed; develop-web-game smoke ran and screenshot was inspected; custom Playwright checks confirmed desktop/mobile ending children stayed inside the card, Ending Save drag preserved width/height exactly (`deltaW=0`, `deltaH=0`), and Export downloaded `index.layout-applied.html` containing `__ratCityEndingCardV12`.
- 2026-05-23: Continued verification after pause. Re-ran module syntax check and `git diff --check`; only CRLF normalization warnings were reported. Re-ran develop-web-game smoke and inspected the loading screenshot; localhost still hits the expected Cloudflare leaderboard CORS fallback. A dedicated Playwright diagnostic with the leaderboard request mocked confirmed desktop/mobile Ending children stay inside `rat-city-panel`, Ending Save drag keeps `deltaW=0` / `deltaH=0`, and `Export` downloads `index.layout-applied.html` containing `__ratCityEndingCardV12`; visual screenshot was inspected, then temporary test artifacts were cleaned up.
- 2026-05-23: Applied the user's exported `index.layout-applied.html` back into `index.html` by replacing only the `UI_LAYOUT_DEFAULTS` block. The exported file and updated `index.html` now hash-match exactly; changed layout targets are Rat City ending/leaderboard objects across desktop and mobile. Verification: extracted module `node --check` passed, `git diff --check` only reported CRLF normalization warning, and develop-web-game smoke screenshot was inspected with only the expected localhost Cloudflare leaderboard CORS fallback.
- 2026-05-24: Added Layout Admin marquee multi-select and group transforms in `index.html`. Dragging empty preview space now draws a cyan selection box and selects all visible unlocked objects it intersects, excluding locked/hidden/wrong-scene/panel-only targets. Dragging any selected object keeps the group together and moves all selected items; resizing a selected group scales each item relative to the group's bounding box. Verification: extracted module script `node --input-type=module --check` passed; dedicated Playwright diagnostic selected 6 unlocked objects, moved all by `24x16`, resized all selected dimensions, and saved `test-results/layout-marquee-group.png`; develop-web-game smoke ran and screenshot was inspected with only the expected localhost Cloudflare leaderboard CORS fallback.
